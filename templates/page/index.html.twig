{% block body%}
    <h1> Tech Blog</h1>
    {% for post in posts %}
        <h2>{{ post.title }}</h2>
        <p>{{ post.content }}</p>
        
    {% endfor %}
    <p> latest articles about IT and development.</p>
{% endblock %}


